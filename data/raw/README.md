# Raw data policy

The original HuffPost JSON Lines dataset is intentionally not committed. The final
project uses the reproducible 1,800-row six-category sample copied from the completed
midterm into `../processed/newsbot_dataset_sample.csv`.

Expected source format when rebuilding the sample: JSON Lines with a documented,
deterministic sampling seed of 42. Do not add personal data or API credentials here.
