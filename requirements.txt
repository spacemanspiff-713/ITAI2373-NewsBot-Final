# Streamlit Community Cloud runtime.
#
# Community Cloud currently builds this app with Python 3.14. Keep this file
# limited to packages imported by streamlit_app.py and its runtime modules.
# The compatible-release ranges let the installer select Python 3.14 wheels
# instead of attempting source builds for the Python 3.12 development pins.
# Use requirements-local.txt for tests, notebooks, reports, Flask, and optional
# transformer models.
streamlit>=1.61,<2
pandas>=3.0,<4
numpy>=2.4,<3
scipy>=1.17,<2
scikit-learn>=1.8,<2
plotly>=6.8,<7
nltk>=3.9,<4
spacy>=3.8.13,<3.9
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
vaderSentiment==3.3.2
networkx>=3.5,<4
# Maintained wheel distribution; it provides the same ``langdetect`` module.
langdetect-py==1.1.1
deep-translator==1.11.4
joblib>=1.5,<2
